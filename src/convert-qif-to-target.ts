import { ConvertTarget } from "./model.js";

export const commandQifToTarget = async (
  sourceQifPath: string,
  target: ConvertTarget,
  columns: string[],
  options: { [name: string]: string }
) => {
 
};