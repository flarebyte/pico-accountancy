/**
 * Responsibilities:
 * - Public module entry that re-exports the core API.
 * - Keeps external consumers decoupled from internal file structure.
 */
export { picoAccountancy } from './accountancy.js';
